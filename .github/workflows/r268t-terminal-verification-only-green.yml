name: R2.68-T Terminal Verification-Only GREEN

on:
  push:
    branches: [hardening-r268t-terminal-verification-only-gpt56sol]
    paths:
      - 'scripts/r268t_apply_terminal_verification_only.py'
      - '.github/workflows/r268t-terminal-verification-only-green.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: r268t-terminal-verification-only-writer
  cancel-in-progress: false

jobs:
  materialize-green:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hardening-r268t-terminal-verification-only-gpt56sol
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: python -m pip install pytest
      - name: Apply minimal terminal-authority fix
        run: PYTHONPATH=. python scripts/r268t_apply_terminal_verification_only.py
      - name: Verify new regression and full R2.68-T focused suite
        run: PYTHONPATH=. python -m pytest -q tests/test_r268_*.py
      - name: Materialize source only on GREEN
        run: |
          if git diff --quiet -- cogcoder/r268_cross_task_causal_transfer.py cogcoder/r268_cross_task_transfer_baseline.py; then
            echo 'Source already materialized; nothing to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add cogcoder/r268_cross_task_causal_transfer.py cogcoder/r268_cross_task_transfer_baseline.py
          git commit -m 'fix: make R2.68-T terminal evidence verification-only'
          git push origin HEAD:hardening-r268t-terminal-verification-only-gpt56sol
