name: Neural vNext Native R7 Dev

on:
  push:
    branches:
      - codex/neural-native-r7-threshold-consistency
    paths:
      - 'model/neural-vnext-native-r7/**'
      - '.github/workflows/neural-vnext-native-r7-dev.yml'
  pull_request:
    paths:
      - 'model/neural-vnext-native-r7/**'
      - '.github/workflows/neural-vnext-native-r7-dev.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  PYTHONHASHSEED: '0'
  OMP_NUM_THREADS: '1'
  MKL_NUM_THREADS: '1'
  OPENBLAS_NUM_THREADS: '1'
  NUMEXPR_NUM_THREADS: '1'
  OMP_DYNAMIC: 'FALSE'
  MKL_DYNAMIC: 'FALSE'
  MKL_CBWR: 'COMPATIBLE'
  KMP_DETERMINISTIC_REDUCTION: 'TRUE'
  ATEN_CPU_CAPABILITY: 'default'

jobs:
  r7-dev:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install pinned neural dependencies
        run: |
          python -m pip install -q pytest numpy
          python -m pip install -q torch==2.9.0 --index-url https://download.pytorch.org/whl/cpu
      - name: Compile and run R7 contracts
        env:
          PYTHONPATH: model/neural-vnext-native-r7:model/neural-vnext-native-r4:model/neural-vnext-native-r3:model/neural-vnext-native-r2:model/neural-vnext-native:model/r1.8
        run: |
          python -m py_compile model/neural-vnext-native-r7/*.py model/neural-vnext-native-r7/scripts/*.py model/neural-vnext-native-r7/tests/*.py
          python -m pytest -q model/neural-vnext-native-r7/tests
      - name: Run frozen-parent R7 dev tournament
        env:
          PYTHONPATH: model/neural-vnext-native-r7:model/neural-vnext-native-r4:model/neural-vnext-native-r3:model/neural-vnext-native-r2:model/neural-vnext-native:model/r1.8
        run: |
          mkdir -p r7-dev
          python model/neural-vnext-native-r7/scripts/train_r7_dev.py             --checkpoint r7-dev/r7.pt             --manifest r7-dev/r7.manifest.json             --dev-result r7-dev/r7.dev.json
      - name: Enforce development-only fresh isolation
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          root = Path('model/neural-vnext-native-r7')
          lock = json.loads((root / 'PREDEV_LOCK.json').read_text())
          gate = json.loads((root / 'FRESH_GATE_PREREGISTRATION.json').read_text())
          manifest = json.loads(Path('r7-dev/r7.manifest.json').read_text())
          assert lock['fresh_isolation']['status'] == 'UNOPENED'
          assert lock['benchmark']['fresh_indices'] == [160, 199]
          assert gate['status'] == 'PREREGISTERED_UNOPENED'
          assert gate['fresh']['opened'] is False
          assert manifest['fresh_opened'] is False
          assert manifest['status'] == 'R7_DEV_ELIGIBLE_FRESH_UNOPENED'
          PY
      - name: Upload frozen development artifact
        uses: actions/upload-artifact@v4
        with:
          name: neural-vnext-native-r7-dev
          path: |
            r7-dev/r7.pt
            r7-dev/r7.manifest.json
            r7-dev/r7.dev.json
          if-no-files-found: error
          retention-days: 90
